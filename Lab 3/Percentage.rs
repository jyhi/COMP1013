fn main() {
    let (mut percentage, mut x): (i32, i32);
    let (mut ch1, mut ch2): (char, char);
    let mut buf = String::new();

    std::io::stdin().read_line(&mut buf).unwrap(); // -> Result<usize>

    // Given up.
}
